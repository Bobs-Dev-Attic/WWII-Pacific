// Single source of truth for the displayed game version.
// Keep in sync with package.json "version" and CHANGELOG.md on every update.
export const VERSION = '1.1.0';
export const BUILD_NAME = 'Glass Cockpit';
export const VERSION_LABEL = `v${VERSION} — "${BUILD_NAME}"`;
